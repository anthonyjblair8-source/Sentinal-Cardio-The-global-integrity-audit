    - name: Execute Integrated Sentinel Orchestrator
      shell: bash
      run: |
        cat << 'EOF' > REPAIR_ORCHESTRATOR.py
        import os
        import json
        import hashlib
        import logging
        from datetime import datetime

        # -- FULL LOGIC START --
        LOG_FILE = "orchestrator_log.log"
        STATE_FILE = "state_registry.json"
        
        logging.basicConfig(level=logging.INFO, format="%(asctime)s [%(levelname)s] %(message)s")

        class LatticeOrchestrator:
            CORE_FILES = ["cli_sentinel.py", "audit_loop.py", "Council_Interface.py"]
            EXPECTED_HASHES = {
                "cli_sentinel.py": "5f4dcc3b5aa765d61d8327deb882cf99",
                "audit_loop.py": "6e6b5e4c3d2a1b0c9d8e7f6a5b4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a7b6",
                "Council_Interface.py": "a1b2c3d4e5f67890123456789abcdef0123456789abcdef0123456789abcdef"
            }

            def __init__(self):
                self.state = "UNKNOWN"

            def run_integrity_check(self):
                print("Running systemic integrity check...")
                all_ok = True
                for file in self.CORE_FILES:
                    if not os.path.exists(file):
                        logging.error(f"Missing: {file}")
                        all_ok = False
                return all_ok

        if __name__ == "__main__":
            orchestrator = LatticeOrchestrator()
            if orchestrator.run_integrity_check():
                print("Lattice SYNCED.")
                exit(0)
            else:
                print("Lattice CORRUPTED.")
                exit(1)
        # -- FULL LOGIC END --
        EOF
        python REPAIR_ORCHESTRATOR.py
